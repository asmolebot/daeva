# daeva-whisper pod package

Example portable pod package for documentation and schema validation.

This directory intentionally shows the proposed Phase 3 package layout without implementing a full installer yet.
